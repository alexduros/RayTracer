# Build output (out-of-source: cmake -B build)
build/
cmake-build-*/

# Legacy in-source build artifacts (the old workflow ran cmake inside src/)
src/CMakeCache.txt
src/CMakeFiles/
src/cmake_install.cmake
src/Makefile
src/compile_commands.json
src/raymini
src/*.o
src/*.a

# Dear ImGui writes its window layout next to the working directory
imgui.ini

# Rendered images from the GUI "Save PNG" button and raymini-cli
renders/

# OS / editor
.DS_Store
*.swp
*.tmp
.claude/settings.local.json

# Personal test models dropped into models/ (not bundled, never committed)
models/cat.*
